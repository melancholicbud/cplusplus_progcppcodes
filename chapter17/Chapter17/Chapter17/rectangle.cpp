//#include "rectangle.h"
//#include <algorithm> // for max function
//
//// Generally for rectangles length >= width, but
//// the constructor doesn't enforce this.
//Rectangle::Rectangle(double len, double wid) : length(len), width(wid) {}
//
//// Length of the longer side--determinde which is longer
//double Rectangle::span() const {
//	return std::max(length, width);
//}
//
//double Rectangle::area() const {
//	return length * width;
//}